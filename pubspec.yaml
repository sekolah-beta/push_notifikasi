name: push_notifikasi
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sensors_plus: ^5.0.1
  flutter_local_notifications: ^17.1.2
  firebase_core: ^3.1.1
  firebase_messaging: ^15.0.2
  rxdart: ^0.27.7
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  change_app_package_name: ^1.3.0
flutter:
  uses-material-design: true